const API = import.meta.env.VITE_API_BASE_URL || import.meta.env.VITE_API_BASE || 'http://localhost:8000';
async function json<T>(path:string, init?:RequestInit):Promise<T>{ const r=await fetch(`${API}${path}`,{headers:{'Content-Type':'application/json',...(init?.headers||{})},...init}); if(!r.ok) throw new Error(await r.text()); return r.json(); }
export type Shot={shot_id:string;title:string;duration:number;visual_prompt:string;action_prompt:string;camera_motion:string;backend:string;status:string;output_video_path?:string};
export type Project={project_id:string;title:string;description?:string;original_prompt?:string;shots:Shot[];generation_settings:any;output_videos:string[]};
export const projectApi={list:()=>json<Project[]>('/api/projects'),create:(p:Partial<Project>)=>json<Project>('/api/projects',{method:'POST',body:JSON.stringify(p)}),get:(id:string)=>json<Project>(`/api/projects/${id}`),update:(id:string,p:any)=>json<Project>(`/api/projects/${id}`,{method:'PUT',body:JSON.stringify(p)}),addShot:(id:string,s:any)=>json<Shot>(`/api/projects/${id}/shots`,{method:'POST',body:JSON.stringify(s)}),updateShot:(id:string,shotId:string,s:any)=>json<Shot>(`/api/projects/${id}/shots/${shotId}`,{method:'PUT',body:JSON.stringify(s)}),removeShot:(id:string,shotId:string)=>json(`/api/projects/${id}/shots/${shotId}`,{method:'DELETE'}),export:(id:string)=>`${API}/api/composer/${id}/export`};
