type Props={value:string;onChange:(v:string)=>void};
const backends=['mock','local_open_video','ltx','wan','cogvideox','hunyuan','comfyui','diffusers_t2v','diffusers_i2v','external'];
export function BackendSelector({value,onChange}:Props){return <div className="panel"><h3>生成后端</h3><select value={value} onChange={e=>onChange(e.target.value)}>{backends.map(b=><option key={b} value={b}>{b}</option>)}</select><p className="muted">不使用外部收费模型：local_open_video / ltx / wan / cogvideox / hunyuan 需本地开源权重与 GPU；mock 仅用于无 GPU 演示。</p></div>}