export function StoryboardPanel({parsed,story}:{parsed:any;story:any}){return <div className="card"><h2>结构化结果</h2><h3>语义解析 JSON</h3><pre className="json">{JSON.stringify(parsed,null,2)}</pre><h3>自动分镜</h3>{story?.shots?.map((s:any)=><div key={s.shot_id} className="card" style={{boxShadow:'none',marginBottom:8}}><b>Shot {s.shot_id} · {s.duration}s</b><p>{s.visual_prompt}</p><small>{s.camera_angle} / {s.camera_motion}</small></div>)}</div>}
