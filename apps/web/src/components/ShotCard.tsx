import type {Shot} from '../lib/projectApi';
import {BackendSelector} from './BackendSelector';

type Props={shot:Shot;index:number;onChange:(patch:Partial<Shot>)=>void;onGenerate:()=>void};
export function ShotCard({shot,index,onChange,onGenerate}:Props){return <div className="shotCard"><div className="shotHead"><b>镜头 {index+1}</b><span>{shot.status}</span></div><label>标题</label><input value={shot.title||''} onChange={e=>onChange({title:e.target.value})}/><label>时长（秒）</label><input type="number" value={shot.duration||4} onChange={e=>onChange({duration:Number(e.target.value)})}/><label>画面 Prompt</label><textarea value={shot.visual_prompt||''} onChange={e=>onChange({visual_prompt:e.target.value})}/><label>动作 Prompt</label><textarea value={shot.action_prompt||''} onChange={e=>onChange({action_prompt:e.target.value})}/><label>运镜</label><input value={shot.camera_motion||'slow_push_in'} onChange={e=>onChange({camera_motion:e.target.value})}/><BackendSelector value={shot.backend||'mock'} onChange={v=>onChange({backend:v})}/><button className="btn mini" onClick={onGenerate}>生成单个镜头</button>{shot.output_video_path&&<p className="muted">输出：{shot.output_video_path}</p>}</div>}
