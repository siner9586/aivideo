# Optional dependencies for local/open-source video generation.
# Install this only on machines intended to run real local video models.
# Choose the correct torch build for your CUDA / ROCm / CPU environment first:
#   https://pytorch.org/get-started/locally/
# Example CUDA install, adjust for your platform:
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio

diffusers>=0.35.0
transformers>=4.45.0
accelerate>=0.34.0
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=5.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
huggingface_hub>=0.24.0
